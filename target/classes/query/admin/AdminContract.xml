<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.workspace.admin.contract.dao.AdminContractDAO">

	<!-- 입주정보 리스트 기본 조회 -->
	<select id="contractInfo" parameterType="member" resultType="List">
		select rownum,
			(select s_name,
				(select comp_name, comp_num
				from member_tbl
				where u_id = #{u_id}),
					start_date, end_date, cont_months, total_cost, payment, cont_state, r_apply, r_state, w_apply, w_state
			from cont_tbl
			order by start_date desc, s_name)
		from cont_tbl
	</select>

	

</mapper>
